<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="uNote.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:uNote.Views"
    Title="uNote">

    <ShellContent ContentTemplate="{DataTemplate views:HomeView}" Route="HomeView" />

    <ShellContent ContentTemplate="{DataTemplate views:NenhumView}" Route="Nenhum" />

</Shell>
